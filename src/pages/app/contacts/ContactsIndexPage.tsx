import React, { FC } from 'react';
import Header from '../../../components/Header';

const ContactsIndexPage: FC = () => (
  <>
    <Header>Contacts</Header>
  </>
);

export default ContactsIndexPage;
